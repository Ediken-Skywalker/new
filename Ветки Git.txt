git branch .... создать ветку
git checkout ... перейти на ветку

создать и сразу же переключиться:

git checkout -b ...... 

git stash - сохраняет незакомиченные изменения и архивирует их git stash pop - вернуть их при возврате на ветку(сохранять можно на одной ветке а вставлять в другую!!!)